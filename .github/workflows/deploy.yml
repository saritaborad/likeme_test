name: Deploy to Apache

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Debug SSH_PRIVATE_KEY
        run: echo "$SSH_PRIVATE_KEY"

      - name: Start SSH Agent
        run: eval "$(ssh-agent -s)"
    
      - name: Add SSH key
        run: ssh-add /home/runner/.ssh/id_rsa

      - name: Debug SSH Configuration
        run: cat /home/runner/.ssh/config


      - name: Configure SSH key
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -t rsa piks.in >> ~/.ssh/known_hosts
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Debug SSH Key Content
        run: cat /home/runner/.ssh/id_rsa

      - name: Debug Known Hosts
        run: cat /home/runner/.ssh/known_hosts


      - name: Set execute permissions for the script
        run: |
          chmod +x ./deploy.sh
          ssh -i /home/runner/.ssh/id_rsa root@piks.in


      - name: Run deployment script
        run: |
          bash ./deploy.sh
        env:
          NODE_ENV: production
